spring:
  data:
    mongodb:
      #host: localhost
      #port: 27017
      #database: smarthouse
      #authentication-database: admin

      uri: mongodb+srv://root:root@cluster0.ltf1m.mongodb.net/smarthouse?retryWrites=true&w=majority
server:
  port: ${PORT:8080}
